#ifndef MACROS_HPP
#define MACROS_HPP

// Macro functions
#define ABS(x) ((x) < 0 ? -(x) : (x))
#define SQR(x) ((x) * (x))
#define CUBE(x) ((x) * (x) * (x))
#define SMALL 1e-8
#define NINT(x) ((x) < 0.0 ? (int)((x)-0.5) : (int)((x) + 0.5))

#endif /* end of include guard: MACROS_HPP */
